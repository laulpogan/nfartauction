import { describe, it, expect } from 'vitest'
import { buildDeck, dealHands, shuffle } from './deck'

describe('buildDeck', () => {
  it.todo('produces exactly 70 cards')
  it.todo('distributes artists correctly')
})

describe('dealHands', () => {
  it.todo('deals 10 cards per player in round 1 for 3 players')
})

describe('shuffle', () => {
  it.todo('returns array of same length')
})
