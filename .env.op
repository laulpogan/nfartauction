# Managed by 1Password (vault Slancha). Run: op run --env-file=.env.op -- <cmd>
# Secrets = op:// refs (synced from this .env on migration). Non-secret config literal.
# Safe to commit. Real .env stays as gitignored fallback until cutover proven.

VITE_PARTYKIT_HOST=nfart-auction.laulpogan.partykit.dev
